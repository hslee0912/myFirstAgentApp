const { validateEmail } = require('./validateEmail');

describe('validateEmail.js (auto-generated smoke test)', () => {
  it('exposes its declared exports', () => {
    expect(typeof validateEmail).toBe('function');
  });
});
